$(document).ready(function() {
   $('.<%= @email.id %>').toggleClass("yellow");
   $('.object').text("<< <%=j @email.object %> >>");
   $('.body').text("<< <%=j @email.body %> >>");

   $('.delete__box').html('<%=j link_to "un lien pour supprimer email", delete_path(@email.id), remote: true, method: "delete" %>');
});

